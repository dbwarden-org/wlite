[package]
name = "wlite"
version = "0.2.0"
edition = "2021"
description = "Rust bindings for libwlite — a tiny SQLite schema and migration engine"
license = "MIT"
links = "wlite"

[build-dependencies]
cc = "1"
pkg-config = "0.3"

[dependencies]
thiserror = "1"
