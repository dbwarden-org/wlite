name: Publish Swift Package

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Tag Swift package
        run: |
          git tag swift/v${{ github.ref_name }}
          git push origin swift/v${{ github.ref_name }}
